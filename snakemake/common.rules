# Common functions

def _get_half_cores(_):
  return int(config["num_threads"]/2)

def _get_all_cores(_):
  return int(config["num_threads"])

rule zip_fastq:
  input:
    "{prefix}.fastq"
  output:
    "{prefix}.fastq.gz"
  shell:
    "gzip {input}"
